diff --git a/dist/core/zip.mjs b/dist/core/zip.mjs
index 181d843e5b5d38d4331b6a3e4ba8973c82fbe9f4..bd867b9a099db016a2e5b2bb17b187ef9efc650a 100644
--- a/dist/core/zip.mjs
+++ b/dist/core/zip.mjs
@@ -63,12 +63,26 @@ async function zip(config) {
 }
 async function zipDir(directory, outputPath, options) {
 	const archive = new JSZip();
-	const filesToZip = [...(await glob(["**/*", ...options?.include || []], {
-		cwd: directory,
-		ignore: ["**/node_modules"],
-		onlyFiles: true,
-		expandDirectories: false
-	})).filter((relativePath) => {
+	let allFiles;
+	try {
+		const { execSync } = await import("node:child_process");
+		const { existsSync } = await import("node:fs");
+		allFiles = execSync("git ls-files --recurse-submodules", {
+			cwd: directory,
+			encoding: "utf-8"
+		}).split("\n").filter((f) => f && existsSync(path.resolve(directory, f)));
+	} catch {
+		allFiles = [];
+	}
+	if (allFiles.length === 0) {
+		allFiles = await glob(["**/*", ...options?.include || []], {
+			cwd: directory,
+			ignore: ["**/node_modules"],
+			onlyFiles: true,
+			expandDirectories: false
+		});
+	}
+	const filesToZip = [...allFiles.filter((relativePath) => {
 		return picomatchMultiple(relativePath, options?.include) || !picomatchMultiple(relativePath, options?.exclude);
 	}), ...(options?.additionalFiles ?? []).map((file) => path.relative(directory, file))];
 	for (const file of filesToZip) {
